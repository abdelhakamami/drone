kind: pipeline
type: kubernetes
name: default

steps:
- name: greeting
  image: alpine
  commands:
  - echo hello
  - echo world
- name: deploy
  image: fluxcd/fluxctl:1.24.1
  enviornment:
     FLUXCD_NAMESPACE: flux-sytem
     KUSTOMIZE_BASE: election-app
  commands:
     - fluxctl --k8s-fwd-ns=flux-system --k8s-fwd-labels app=source-controller list-workloads --all-namespaces
