kind: pipeline
name: default
type: kubernetes

steps:
- name: test
  image: plugins/docker
  commands:
     - apk add --update curl jq
     - docker login -u $UNAME -p $TOKEN
     - chmod +x remove_images.sh
     - ./remove_images.sh
