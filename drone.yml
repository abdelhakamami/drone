kind: pipeline
name: default

steps:
- name: test
  image: docker:dind
  volumes:
  - name: dockersock
    path: /var/run
  commands:
  - apk add --no-cache curl jq
  - chmod +x remove_images.sh
  - ./remove_images.sh
 
  

services:
- name: docker
  image: docker:dind
  privileged: false
  volumes:
  - name: dockersock
    path: /var/run

volumes:
- name: dockersock
  temp: {}
    

    
  
